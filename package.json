{
  "name": "github-opencode-agent-glue",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "GitHub Actions glue for an issue-to-PR opencode agent with Langfuse workflow tracing.",
  "scripts": {
    "check": "mise run check",
    "test": "mise run test",
    "worker:build": "mise run worker:build",
    "worker:dev": "mise run worker:dev",
    "worker:test": "mise run worker:test",
    "worker:test:opencode": "mise run worker:test:opencode",
    "worker:docker:build": "mise run worker:docker:build",
    "worker:smoke:container": "mise run worker:smoke:container",
    "worker:test:opencode-container": "mise run worker:test:opencode-container",
    "worker:opencode:smoke": "mise run worker:opencode:smoke"
  },
  "engines": {
    "node": ">=20"
  }
}
