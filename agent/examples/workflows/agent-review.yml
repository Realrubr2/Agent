name: Agent Review

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

jobs:
  agent:
    uses: Realrubr2/Agent/.github/workflows/agent-review.yml@v1
    with:
      model: anthropic/claude-sonnet-4-5
      review_model: anthropic/claude-opus-4-5
    secrets: inherit
