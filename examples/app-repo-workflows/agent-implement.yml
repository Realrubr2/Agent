name: Agent Implement

on:
  issue_comment:
    types: [created]

permissions:
  id-token: write
  contents: write
  issues: write
  pull-requests: write
  actions: read

jobs:
  implement:
    uses: realrubr/Agent/.github/workflows/reusable-implement.yml@main
    secrets: inherit
