name: Agent Implement

on:
  issue_comment:
    types: [created]

permissions:
  contents: write
  issues: write
  pull-requests: write
  actions: read

jobs:
  implement:
    uses: realrubr2/Agent/.github/workflows/reusable-implement.yml@main
    secrets: inherit
