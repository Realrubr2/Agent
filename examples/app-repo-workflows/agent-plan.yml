name: Agent Plan

on:
  issues:
    types: [opened]
  issue_comment:
    types: [created]

permissions:
  id-token: write
  contents: read
  issues: write
  actions: read

jobs:
  plan:
    uses: realrubr/Agent/.github/workflows/reusable-plan.yml@main
    secrets: inherit
