name: Agent Review

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

jobs:
  agent:
    uses: Realrubr2/Agent/.github/workflows/agent-review.yml@v1
    with:
      model: openrouter/openai/gpt-5.2
      review_model: openrouter/openai/gpt-5.2
    secrets: inherit
